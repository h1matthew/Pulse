# UI Simplification Pass

## Goal

Make the public landing UI feel quieter, simpler, and less AI-generated by reducing copy, removing decorative glow-heavy presentation, and using product-like local business panels.

## Plan

- [x] Inspect current homepage, shared home components, theme, and tests.
- [x] Add focused regression coverage for shorter homepage copy and removed AI-heavy phrases.
- [x] Replace the long hero and explanatory sections with a compact landing flow.
- [x] Simplify the interactive feature/demo component copy and visuals.
- [x] Run focused tests.
- [x] Run full test suite and production build.
- [x] Open the page locally and check desktop/mobile visuals.

## Scope

- Edit the homepage, shared home presentation components, and header/NavLink markup needed to remove invalid nested controls.
- Preserve existing navigation, auth provider behavior, data hooks, and public routes.
- Keep the design aligned with the existing dark Pulse theme while removing purple/teal glow-heavy styling.

## Review

- Simplified the landing page into a quieter local guide with short copy, fewer sections, no glow blobs, no gradient text, and no AI-heavy phrasing.
- Reworked the home feature tabs into a compact local snapshot with Find, Deals, and Impact states.
- Made `HomeWrapper` consistently dark so the homepage does not render white text on a light surface.
- Updated `AnimatedSection` to keep content visible before the intersection observer fires; this fixed blank browser screenshots.
- Fixed invalid header and homepage link/button nesting by using styled links for navigation CTAs.
- Added/updated tests for homepage copy, home wrapper, feature tabs, animated visibility, accessibility labels, and header link markup.

## Verification

- Passed: `cd /Users/brady/Code/Pulse/web && npm run test:run -- app/__tests__/page.test.tsx app/__tests__/accessibility.test.tsx components/features/home/__tests__/AnimatedSection.test.tsx components/features/home/__tests__/FeatureTabs.test.tsx components/features/home/__tests__/HomeWrapper.test.tsx components/features/home/__tests__/CommunityStatsIsland.test.tsx`
- Passed: `cd /Users/brady/Code/Pulse/web && npm run test:run -- components/layout/__tests__/Header.test.tsx`
- Passed: `cd /Users/brady/Code/Pulse/web && npm run build`
- Visual check passed with Playwright screenshots:
  - `web/output/playwright/pulse-home-desktop.png`
  - `web/output/playwright/pulse-home-mobile.png`
- `npm run lint` exits 0 but currently prints ESLint help, so it is not a meaningful lint signal.
- Full suite still fails outside this UI pass: `7 failed | 68 passed`, with failures in Gemini model expectation, Google photo/nearby API tests, QueryProvider defaults, review route Supabase mock, BusinessCard tag casing, business detail review submission, and an AuthProvider mock unhandled upsert.

---

# Discover Page Improvement Pass

## Goal

Make `/discover` feel like a practical local directory: faster to scan, less wordy, less decorative, and clearer about location, filters, and results.

## Plan

- [x] Inspect current `/discover` page, local card implementation, shared discover card, constants, and tests.
- [x] Add focused regression coverage for the simplified discover page surface.
- [x] Simplify the top area from a marketing hero into a compact directory header.
- [x] Make search, sort, category, location, and radius controls easier to scan.
- [x] Reduce card visual noise and make result metadata more useful.
- [x] Remove the verbose “How Pulse Works” explainer from the bottom of `/discover`.
- [x] Run focused discover tests, build, and browser screenshots.
- [x] Document verification results here.

## Scope

- Edit `web/app/discover/page.tsx` and discover-specific tests only unless a small shared fix is required.
- Preserve current data hooks, location behavior, category filtering, sorting, bookmark flow, and routes.

## Review

- Reworked `/discover` from a large marketing hero into a compact directory page with one headline, short supporting copy, summary chips, and a single control panel.
- Removed emoji category labels, decorative gradient/orb treatment, photo overlays, generated business summaries, and the bottom “How Pulse Works” explainer.
- Made business result cards more factual and scannable: name, category, rating, review label, price, distance, location, and a small tag row.
- Moved the bookmark action outside the card link to avoid nesting an interactive button inside a navigation link.
- Removed entry animations from the primary discover controls so search and filters are readable immediately on first paint and in screenshots.
- Added loading-safe summary labels so the header shows “Finding places” instead of reporting false zero-result metrics while the nearby query is still loading.

## Verification

- Passed: `cd /Users/brady/Code/Pulse/web && npm run test:run -- app/discover/__tests__/page.test.tsx`
- Passed: `cd /Users/brady/Code/Pulse/web && npm run build`
- Visual check passed with Playwright screenshots:
  - `web/output/playwright/pulse-discover-desktop.png`
  - `web/output/playwright/pulse-discover-mobile.png`
- Full suite still fails outside this discover pass: `7 failed | 69 passed`, with failures in Gemini model expectation, Google nearby/photo API tests, review route Supabase mock/profile chain, QueryProvider defaults, shared discover BusinessCard tag casing, business detail review submission, and an AuthProvider mock unhandled upsert.

---

# Onboarding Tour Stall Fix

## Goal

Make the first-run Pulse onboarding tour feel quick and prevent it from getting stuck on "Taking you there".

## Plan

- [x] Review relevant lessons and current onboarding-tour behavior.
- [x] Reproduce or isolate the slow/stuck "Taking you there" state.
- [x] Add a focused failing regression test for the stuck transition.
- [x] Implement the smallest root-cause fix in the tour lifecycle.
- [x] Run focused tests and app verification.
- [x] Document review and verification results here.

## Scope

- Target the guided onboarding tour only unless verification proves the page anchors are part of the root cause.
- Preserve the real-control spotlight behavior: users should still be able to type in search and click highlighted controls.

## Review

- Removed the extra scroll-settle gate in `OnboardingTour`, so the tour reveals a found target immediately and lets the existing frame tracker keep the spotlight attached while the page scrolls.
- Added regression coverage that fails if an already-rendered target stays stuck on "Taking you there" after the target poll finds it.
- Updated the older settling test to assert loading copy only while the target has not appeared.

## Verification

- Watched the new onboarding regression fail before implementation.
- Passed: `cd /Users/brady/Code/Pulse/web && npm run test:run -- components/features/help/__tests__/OnboardingTour.test.tsx`
- Passed: `cd /Users/brady/Code/Pulse/web && npm run test:run`
- Passed: `cd /Users/brady/Code/Pulse/web && npm run build`
- Chrome verification passed on `http://localhost:3000`: search, category, and business-card tour steps now unlock on the next 100 ms sample after their targets appear, and "Open it for me" reaches the reviews step without trapping the tour.
